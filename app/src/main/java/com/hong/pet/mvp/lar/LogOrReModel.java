package com.hong.pet.mvp.lar;

import com.hong.pet.base.BaseModel;

public class LogOrReModel extends BaseModel<LogOrRePresenter> {

    public LogOrReModel(LogOrRePresenter logOrRePresenter) {
        super(logOrRePresenter);
    }
}
